# 图书借阅管理系统 产品需求文档（PRD）

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 简易 PC 端图书借阅管理系统 PRD |
| 文档版本 | V1.3 |
| 编写日期 | 2026-09-06 |
| 文档状态 | 假设项已确认 |
| 产品形态 | PC Web 端（前后端分离） |
| 目标用户 | 图书馆管理员、普通读者 |

---

## 2. 项目概述

### 2.1 项目背景

传统图书馆的图书登记、借阅与归还依赖人工台账，存在查询效率低、库存状态不准确、借阅记录难以追溯等问题。本项目建设一套**简易的 PC 端图书借阅管理系统**，将图书馆藏管理与借还流程线上化，帮助管理员高效维护图书、掌握全馆借阅情况，同时让读者能够便捷地查询图书并完成借书、还书操作。

### 2.2 产品目标

- **管理员侧**：完成图书馆藏的新增、编辑、删除；查看全馆所有借阅记录；快速处理读者还书。
- **读者侧**：浏览与查询馆藏图书；在线借书与还书；随时查看个人借阅记录与归还期限。
- **系统侧**：库存数量随借还操作准确变动；借阅状态清晰可追溯；不同角色权限隔离。

### 2.3 项目范围

**本期范围内（In Scope）**

- PC Web 端，面向桌面浏览器使用；
- 用户登录认证，区分**管理员**与**读者**两种角色；
- 管理员：图书新增、编辑、删除；图书分类管理（新增、修改、删除）；查看所有借阅记录；处理还书；
- 读者：浏览图书列表、查询图书、借书、查看本人借阅记录、还书；
- 读者自助注册（仅可注册读者角色）；
- 借阅库存联动、逾期状态识别等基础业务规则。

**本期范围外（Out of Scope）**

- 手机移动端 / 小程序适配（本期仅 PC 端）；
- 罚金计算与在线支付（本期仅做逾期状态标记）；
- 图书采购、入库审批、库存盘点等延伸业务；
- 短信 / 邮件到期提醒、消息推送；
- 图书评论、评分、推荐等社交化功能。

---

## 3. 用户角色与权限

### 3.1 角色定义

| 角色 | 角色标识 | 说明 |
| --- | --- | --- |
| 管理员 | `admin` | 图书馆工作人员，负责馆藏图书维护与借还业务管理 |
| 读者 | `reader` | 注册读者，可查询图书并进行借书、还书操作 |

### 3.2 权限矩阵

| 功能 | 管理员 | 读者 |
| --- | :---: | :---: |
| 登录 / 退出系统 | ✅ | ✅ |
| 查看个人资料 / 退出登录 | ✅ | ✅ |
| 浏览图书列表、查询图书 | ✅ | ✅ |
| 借书 | ❌ | ✅ |
| 收藏图书 / 查看我的收藏 | ❌ | ✅ |
| 还书（对本人借阅记录） | ❌ | ✅ |
| 查看本人借阅记录 | ✅（本人作为读者的记录，可选） | ✅ |
| 查看**全部**借阅记录 | ✅ | ❌ |
| 处理还书（代读者办理归还） | ✅ | ❌ |
| 新增图书 | ✅ | ❌ |
| 编辑图书 | ✅ | ❌ |
| 删除图书 | ✅ | ❌ |
| 分类管理（新增/修改/删除分类） | ✅ | ❌ |
| 读者注册 | ❌ | ✅（仅可注册为读者） |

> 权限控制要求：前端按角色渲染菜单与按钮，后端接口必须同步做角色鉴权，不能仅依赖前端隐藏。

---

## 4. 功能需求

### 4.1 公共功能：登录与认证

| 编号 | 功能 | 需求描述 |
| --- | --- | --- |
| FR-101 | 用户登录 | 用户使用「用户名 + 密码」登录；系统校验账号密码并识别角色，登录成功后进入对应角色首页。账号或密码错误时给出明确提示（如「用户名或密码错误」），连续失败不做验证码（简易系统）。 |
| FR-102 | 退出登录 | 用户可主动退出，退出后清除登录态并返回登录页。 |
| FR-103 | 登录态与权限控制 | 登录后持有会话凭证（Token）；未登录访问任意业务页跳转登录页；读者访问管理员页面/接口时被拒绝并提示无权限。 |
| FR-104 | 读者注册 | 登录页提供「读者注册」入口，注册页**仅提供读者注册选项**，不支持注册管理员账号。填写用户名、密码、确认密码、姓名等信息即可注册，注册成功后角色固定为**读者**。用户名不可重复。 |
| FR-105 | 初始管理员账号 | 系统初始化时由部署方配置一个管理员账号（如通过数据库初始化脚本或环境变量），用于首次登录。**网页端不提供管理员注册入口**。首次登录后建议修改密码。 |

### 4.2 管理员功能

#### 4.2.1 图书管理

| 编号 | 功能 | 需求描述 |
| --- | --- | --- |
| FR-201 | 图书列表 | 分页展示全部图书，显示书名、作者、ISBN、分类、总馆藏数、可借数量、状态等；支持按书名/作者/ISBN 关键词搜索与按分类筛选。 |
| FR-202 | 新增图书 | 录入图书信息：书名（必填）、作者（必填）、ISBN（必填且唯一）、分类（必填，从已有分类中选择）、出版社、出版年份、总馆藏数量（必填，≥1）、简介、封面图片（选填）。提交时做必填与格式校验；ISBN 重复时提示不可重复添加。新增成功后「可借数量 = 总馆藏数量」。封面图片支持两种方式：①管理员本地上传 jpg/png 图片（限制文件大小 ≤ 2MB），上传成功后返回图片访问 URL；②直接填写图片 URL。 |
| FR-203 | 编辑图书 | 可修改图书全部信息；调整总馆藏数量时，可借数量按规则联动（见 BR-08）。 |
| FR-204 | 删除图书 | 对图书执行删除前弹出二次确认；当该书仍存在「借阅中」的记录时**禁止删除**并提示原因（如「该图书尚有 N 册未归还，无法删除」）。删除后该书不再出现在读者图书列表中。删除图书时**不清理磁盘上的旧封面图片文件**（仅删除数据库记录）。 |

#### 4.2.2 借阅管理

| 编号 | 功能 | 需求描述 |
| --- | --- | --- |
| FR-205 | 全部借阅记录 | 分页展示全馆所有借阅记录，包含：读者用户名/姓名、图书书名、借出时间、应还时间、实际归还时间、当前状态（借阅中 / 已归还 / 逾期）。支持按状态、读者关键词、图书关键词、借出时间范围筛选。 |
| FR-206 | 处理还书 | 管理员可在借阅记录中对「借阅中 / 逾期」状态的记录执行「办理归还」：确认后记录实际归还时间、状态置为「已归还」、该图书可借数量 +1。操作结果给出成功提示。 |
| FR-207 | 逾期识别 | 借阅记录中，当「应还时间 < 当前时间 且 未归还」时，状态显示为「逾期」，并在记录中突出标识。 |

#### 4.2.3 分类管理

| 编号 | 功能 | 需求描述 |
| --- | --- | --- |
| FR-208 | 分类列表 | 管理员可查看全部分类列表，显示分类名称、该分类下图书数量、创建时间等。 |
| FR-209 | 新增分类 | 录入分类名称（必填，唯一），提交时校验名称不可与已有分类重复。 |
| FR-210 | 修改分类 | 可修改分类名称；修改后所有关联图书的分类显示同步更新。 |
| FR-211 | 删除分类 | 删除分类前弹出二次确认；**当该分类下仍绑定有图书时禁止删除**并提示原因（如「该分类下有 N 本图书，无法删除」）。仅当分类下无图书时方可删除。 |

### 4.3 读者功能

| 编号 | 功能 | 需求描述 |
| --- | --- | --- |
| FR-301 | 浏览图书列表 | 分页展示全部在架图书，显示书名、作者、分类、可借数量等信息；可借数量为 0 时明确展示「已借完」，借书按钮置灰。 |
| FR-302 | 查询图书 | 支持按书名 / 作者 / ISBN 关键词搜索，并可按分类筛选；支持分页浏览查询结果。 |
| FR-303 | 查看图书详情 | 可查看图书完整信息（书名、作者、ISBN、分类、出版社、出版年份、简介、总馆藏数、可借数）。 |
| FR-304 | 借书 | 在图书列表/详情页点击「借阅」：系统校验通过后生成借阅记录（借出时间=当前时间，应还时间=当前时间+借期），该图书可借数量 -1。校验不通过时给出明确原因（见第 9 节业务规则）。 |
| FR-305 | 我的借阅记录 | 分页展示**本人**的全部借阅记录，包含图书信息、借出时间、应还时间、实际归还时间、状态；借阅中/逾期记录突出展示应还日期。仅能看到本人记录。 |
| FR-306 | 还书 | 读者对本人「借阅中 / 逾期」状态的记录点击「归还」：二次确认后完成还书，记录实际归还时间、状态置为「已归还」、可借数量 +1。 |
| FR-307 | 逾期提示 | 读者借阅记录中对逾期记录进行醒目标识（如红色「已逾期」标签）。 |
| FR-308 | 收藏图书 | 读者可在图书详情页点击「收藏 / 取消收藏」，收藏状态明确展示（如红色心形「已收藏」），支持切换。 |
| FR-309 | 我的收藏 | 读者可在「我的收藏」页面查看所有已收藏图书，支持取消收藏、跳转图书详情。 |
| FR-310 | 个人资料与退出登录 | 读者点击右上角头像进入个人资料页，可查看用户名、姓名、手机号、邮箱；其中用户名和姓名不可修改，手机号和邮箱可编辑保存；页面提供退出登录按钮，点击后清除登录态并跳转登录页。 |

---

## 5. 核心业务流程

### 5.1 借书流程（读者）

1. 读者登录后进入图书列表页，浏览或搜索目标图书；
2. 点击「借阅」按钮；
3. 系统依次校验：
   - 该图书可借数量是否 > 0；
   - 该读者当前借阅中（含逾期）图书数量是否已达上限（BR-01）；
   - 该读者是否存在逾期未还图书（BR-04）；
4. 校验通过 → 创建借阅记录（状态：借阅中，借出时间=现在，应还时间=现在+30 天），图书可借数量 -1，提示「借阅成功」；
5. 校验不通过 → 弹窗提示具体原因（如「该书已借完」「您有逾期未还图书，暂不可借阅」），不生成记录。

### 5.2 还书流程

**读者自助还书：**

1. 读者进入「我的借阅」，找到状态为「借阅中/逾期」的记录；
2. 点击「归还」→ 二次确认；
3. 系统写入实际归还时间，状态置为「已归还」，图书可借数量 +1；
4. 提示「归还成功」。

**管理员处理还书：**

1. 管理员进入「借阅记录」页，通过读者/图书/状态筛选定位目标借阅记录；
2. 点击「办理归还」→ 确认；
3. 系统完成同样的归还处理（记录归还时间、状态变更、库存 +1）。

> 说明：同一条借阅记录只能归还一次；已归还记录不再显示归还操作入口。

### 5.3 图书新增 / 编辑 / 删除流程（管理员）

1. **新增**：管理员填写图书表单 → 前端必填/格式校验 → 后端校验 ISBN 唯一性 → 写入图书，可借数量初始化为总馆藏数 → 列表中可见。
2. **编辑**：管理员修改字段 → 校验通过后保存；总馆藏数调整时按 BR-08 联动可借数量。
3. **删除**：管理员点击删除 → 系统检查是否存在「借阅中」记录 → 存在则禁止并提示；不存在则二次确认后删除。

---

## 6. 数据模型（核心实体）

以下为产品层面的核心实体与关键字段，供设计与开发对齐：

### 6.1 用户（user）

| 字段 | 说明 |
| --- | --- |
| id | 用户唯一标识 |
| username | 登录用户名，唯一 |
| password | 登录密码（哈希加密存储，不存明文） |
| role | 角色：`admin` / `reader` |
| real_name | 姓名 |
| phone | 手机号（必填） |
| email | 邮箱（必填） |
| created_at | 创建时间 |

### 6.2 分类（category）

| 字段 | 说明 |
| --- | --- |
| id | 分类唯一标识 |
| name | 分类名称（必填，唯一，如：文学、科技、历史等） |
| created_at / updated_at | 创建 / 更新时间 |

### 6.3 图书（book）

| 字段 | 说明 |
| --- | --- |
| id | 图书唯一标识 |
| title | 书名（必填） |
| author | 作者（必填） |
| isbn | ISBN 编号（必填，唯一） |
| category_id | 分类 ID（外键，关联 category 表；不再使用文本字典） |
| publisher | 出版社 |
| publish_year | 出版年份 |
| total_count | 总馆藏数量（≥1） |
| available_count | 可借数量（随借还变动，0 ≤ available_count ≤ total_count） |
| summary | 简介（选填） |
| cover_url | 封面图片地址（选填；本地上传后存为服务器可访问的 URL） |
| created_at / updated_at | 创建 / 更新时间 |

### 6.4 借阅记录（borrow_record）

| 字段 | 说明 |
| --- | --- |
| id | 记录唯一标识 |
| user_id | 借阅读者（关联 user） |
| book_id | 所借图书（关联 book） |
| borrow_time | 借出时间 |
| due_time | 应还时间（借出时间 + 借期） |
| return_time | 实际归还时间（未归还时为空） |
| status | 状态：`borrowed` 借阅中 / `returned` 已归还 / `overdue` 逾期 |
| operator_id | 办理归还的操作人（读者自助=本人；管理员办理=对应管理员） |

### 6.5 收藏记录（favorite）

| 字段 | 说明 |
| --- | --- |
| id | 收藏记录唯一标识 |
| user_id | 收藏读者（关联 user） |
| book_id | 被收藏图书（关联 book） |
| created_at | 收藏时间 |

**实体关系：**

- 一个分类可对应多本图书（1:N）；
- 一个读者可对应多条借阅记录（1:N）；
- 一本图书可对应多条借阅记录（1:N，含历史记录）；
- 读者与图书通过收藏记录形成多对多关系（M:N）；同一读者对同一图书仅有零或一条收藏记录（见 BR-13）；
- 借阅记录是借还业务与库存变动的唯一凭据。

---

## 7. 页面结构（PC 端）

系统采用左右布局（侧边导航栏 + 顶部用户栏 + 内容区），适配桌面浏览器，**最小支持分辨率 1280×720，不做移动端适配**。

| 页面 | 访问角色 | 核心内容 |
| --- | --- | --- |
| 登录页 | 所有访客 | 用户名、密码输入框；登录按钮；读者注册入口 |
| 注册页 | 所有访客 | 用户名、密码、确认密码、姓名等注册表单 |
| 管理员 - 图书管理页 | 管理员 | 图书表格（分页、搜索、分类筛选）；新增/编辑/删除按钮与弹窗表单 |
| 管理员 - 借阅记录页 | 管理员 | 全部借阅记录表格（分页、状态/读者/图书/时间筛选）；办理归还按钮 |
| 读者 - 图书浏览页 | 读者 | 图书表格/卡片列表（分页、关键词搜索、分类筛选）；借阅按钮、详情入口 |
| 读者 - 图书详情 | 读者 | 图书完整信息、可借数量、借阅按钮（弹窗或独立页均可） |
| 读者 - 我的借阅页 | 读者 | 本人借阅记录表格（分页、状态筛选）；归还按钮；逾期标识 |
| 读者 - 我的收藏页 | 读者 | 已收藏图书列表（封面、书名、作者、分类、可借数量、收藏时间）；取消收藏按钮、详情入口 |
| 读者 - 个人资料页 | 读者 | 用户名（只读）、姓名（只读）、手机号、邮箱输入框；保存按钮；退出登录按钮 |

**交互通用要求：**

- 所有表单提交前做前端校验，错误信息在字段旁明确提示；
- 删除、还书等不可逆/关键操作需二次确认弹窗；
- 操作成功/失败均以全局消息提示（Element Plus Message / MessageBox）；
- 列表数据均采用分页 + 条件筛选，避免一次性加载全量数据。

---

## 8. 非功能需求

### 8.1 技术栈约束

| 层 | 技术选型 |
| --- | --- |
| 前端 | Vue 3 + TypeScript + Element Plus（PC 端组件库） |
| 后端 | Python FastAPI |
| 数据库 | MySQL |
| 架构 | 前后端分离，RESTful 接口；登录认证采用 Token（JWT）方案 |

### 8.2 其他非功能要求

- **性能**：常规列表查询与借还操作在低并发教学/馆内场景下响应时间 ≤ 1 秒；列表必须分页。
- **安全性**：密码哈希存储；后端接口按角色强制鉴权；使用 ORM 参数化查询防止 SQL 注入；前端输出转义防止 XSS。
- **兼容性**：支持 Chrome、Edge 桌面浏览器最近两个大版本；不要求适配移动端浏览器。
- **易用性**：界面元素清晰不重叠；操作路径短（借书/还书均 ≤ 3 次点击）；错误提示语义明确。
- **文件上传**：图书封面上传限制 jpg/png 格式、单文件 ≤ 2MB；上传文件存于服务器指定目录，通过静态资源 URL 访问。
- **数据一致性**：借还操作中「借阅记录写入」与「可借数量变更」必须保证事务一致性，不允许出现记录生成但库存未扣减等情况。

---

## 9. 业务规则汇总

| 编号 | 规则 |
| --- | --- |
| BR-01 | 每位读者同时处于「借阅中 + 逾期」状态的图书最多 **5 册**（参数可配置）。 |
| BR-02 | 单次借期为 **30 天**，应还时间 = 借出时间 + 30 天（参数可配置）。 |
| BR-03 | 图书可借数量为 0 时，读者不能借阅该书，借阅按钮置灰。 |
| BR-04 | 读者存在逾期未还图书时，不可再借阅任何图书，需先归还逾期图书。 |
| BR-05 | 图书存在「借阅中/逾期」记录时不可删除，仅可编辑；无未还记录后方可删除。 |
| BR-06 | 一条借阅记录对应一册图书的一次借出；同一读者可对同一图书产生多条历史借阅记录。 |
| BR-07 | 逾期状态判定：应还时间早于当前时间且未归还。系统在查询记录时实时判定（或定时任务刷新），无需人工标记。 |
| BR-08 | 编辑图书总馆藏数时：新总数 ≥（总馆藏数 − 可借数量，即已借出数量）方可保存；新增馆藏时可借数量同步增加对应册数。 |
| BR-09 | 借阅记录一经生成不可删除；归还后记录永久保留用于追溯。 |
| BR-10 | **逾期不拦截还书操作**：读者存在逾期图书时仍可正常归还逾期图书及其他图书；逾期仅阻止借书（见 BR-04），不阻止还书。 |
| BR-11 | 删除分类时，若该分类下仍绑定有图书，则**禁止删除**并提示原因；仅当分类下无图书时方可删除。 |
| BR-12 | 图书封面本地上传仅支持 jpg / png 格式，单文件大小 ≤ 2MB；上传后图片存于服务器指定目录，以可访问 URL 形式存入 book.cover_url。删除图书时不清理磁盘上的旧封面图片文件。 |
| BR-13 | 同一读者对同一本图书只能收藏一次；重复点击「收藏」等效于取消收藏（收藏/取消为切换操作）。 |
| BR-14 | 图书删除后，对应的收藏记录**级联删除**。 |
| BR-15 | 个人资料页中用户名和姓名不可修改，仅手机号和邮箱可编辑保存。 |
| BR-16 | 手机号格式校验（11位数字），邮箱格式校验（含@符号）。 |
| BR-17 | 点击退出登录后清除本地登录态（Token），跳转至登录页，不可通过浏览器后退回到业务页面。 |

---

## 10. 验收标准

### 10.1 登录与权限

- [ ] 管理员、读者均可用正确账号密码登录，并进入各自角色的首页；
- [ ] 错误密码登录被拒绝并提示；未登录访问业务页被重定向到登录页；
- [ ] 读者无法看到管理员菜单，且直接调用管理员接口返回无权限；
- [ ] 读者可通过注册页创建账号，注册页仅可注册为读者角色（无角色选择项），注册后可正常登录；
- [ ] 网页端不提供管理员注册入口；管理员账号仅由部署方初始化配置。

### 10.2 管理员功能

- [ ] 可新增图书，必填项为空或 ISBN 重复时被阻止并提示；分类字段从已有分类下拉选择；
- [ ] 图书封面支持本地上传 jpg/png（≤ 2MB）和填写 URL 两种方式，上传后可通过 URL 访问；
- [ ] 可编辑图书信息，调整总馆藏数时按 BR-08 联动；
- [ ] 存在未归还记录的图书删除被阻止并提示原因；无未还记录的图书可经二次确认后删除；删除图书时磁盘旧封面文件不被清理；
- [ ] 可新增分类，名称重复时被阻止并提示；可修改分类名称，关联图书显示同步更新；
- [ ] 有图书绑定的分类删除被阻止并提示原因；无图书绑定的分类可经二次确认后删除；
- [ ] 可查看全馆所有借阅记录，并可按状态/读者/图书/时间筛选；
- [ ] 可对借阅中/逾期记录办理归还，归还后状态变为「已归还」、可借数量 +1。

### 10.3 读者功能

- [ ] 可分页浏览图书列表、按关键词（书名/作者/ISBN）与分类查询；
- [ ] 可借数量为 0 的图书显示「已借完」且无法借阅；
- [ ] 满足借书条件时借阅成功：生成借阅记录、可借数量 −1、应还时间为 30 天后；
- [ ] 达到借阅上限或存在逾期图书时，借书被拒绝并提示具体原因；
- [ ] 「我的借阅」仅展示本人记录，可对借阅中/逾期记录归还（逾期不拦截还书），归还后状态与库存正确更新；
- [ ] 逾期记录在读者端与管理员端均有醒目标识；
- [ ] 可收藏 / 取消收藏图书，收藏状态明确展示；「我的收藏」页展示已收藏列表，支持取消收藏与跳转详情；
- [ ] 点击右上角头像可进入个人资料页，用户名和姓名只读，手机号和邮箱可修改保存；
- [ ] 点击退出登录后跳转登录页，浏览器后退无法回到业务页面。

### 10.4 非功能

- [ ] 系统在 Chrome / Edge 最新版本下页面元素清晰、无重叠、无错位；
- [ ] 列表分页正常，常规操作响应 ≤ 1 秒；
- [ ] 借还操作后库存数量与记录状态始终一致（事务保证）。

---

## 11. 假设与确认项

> 以下假设项已全部经用户确认，不再作为待确认事项。

| 编号 | 事项 | 确认结论 |
| --- | --- | --- |
| A-01 | 读者账号来源 | **读者自助注册**，保留注册页。注册页**仅提供读者注册选项**，不支持注册管理员。 |
| A-02 | 逾期罚金 | 本期**不做罚金**，仅标记逾期状态。**逾期不拦截还书操作**，读者仍可归还逾期图书。 |
| A-03 | 图书封面 | 管理员支持**本地上传 jpg/png 图片**（限制文件大小 ≤ 2MB），同时保留填写 URL 备选方式。**删除图书时不清理磁盘旧图片文件**。 |
| A-04 | 借阅上限与借期 | 借阅上限 **5 册**、借期 **30 天**，设计为**可配置参数**。 |
| A-05 | 初始管理员账号 | 由**部署方配置**（数据库初始化脚本或环境变量），**网页端不提供管理员注册入口**。 |
| A-06 | 图书分类 | **增加分类管理页面**，管理员支持新增、修改、删除分类。分类下已绑定图书时**禁止删除**。**新建独立 category 数据表**，图书表使用 **category_id 外键关联**，不再使用固定字典文本。 |

---

## 12. 后续迭代建议（非本期）

- 逾期罚金计算与缴费记录；
- 到期 / 逾期提醒（站内消息、邮件）；
- 借阅数据统计看板（热门图书、借阅趋势）；
- 移动端适配与小程序。
